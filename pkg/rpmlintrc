# -*- python -*-

from Config import addFilter

# Pour xmllint
addFilter("[EW]: explicit-lib-dependency libxml2(-utils)?")

# Verrou accessible depuis le groupe (#1518)
addFilter("E: non-standard-dir-perm /var/lock/vigilo-vigiconf 0775L")

